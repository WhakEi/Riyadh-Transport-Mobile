<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RiyadhTransport"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RiyadhTransport.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".StationDetailsActivity"
            android:label="@string/station_details"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".LineDetailsActivity"
            android:label="@string/line_details"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".RouteDetailsActivity"
            android:label="@string/route_details"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".LineStationsActivity"
            android:label="@string/line_details"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".StationLinesActivity"
            android:label="@string/station_lines"
            android:parentActivityName=".LineStationsActivity" />
            
        <activity
            android:name=".SearchLocationActivity"
            android:windowSoftInputMode="stateAlwaysVisible"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".FavoritesActivity"
            android:label="@string/favorites"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>
